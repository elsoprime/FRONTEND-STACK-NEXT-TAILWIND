post:
  tags:
    - Auth
  summary: Re-dispatch email verification instructions without revealing account state
  operationId: resendEmailVerification
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/auth.yaml#/ResendVerificationRequest
  responses:
    '202':
      description: Request accepted. If the account is eligible, verification instructions are delivered through a secure external channel.
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/auth.yaml#/EmailVerificationDispatchAcceptedResponse
    '400':
      $ref: ../../components/responses/_index.yaml#/GenericError
