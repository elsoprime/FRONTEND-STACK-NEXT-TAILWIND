Tenant:
  type: object
  required:
    - id
    - name
    - slug
    - status
    - subscriptionStatus
    - ownerUserId
    - planId
    - activeModuleKeys
    - memberLimit
  properties:
    id:
      type: string
    name:
      type: string
    slug:
      type: string
    status:
      type: string
      enum: [active, suspended]

    subscriptionGraceEndsAt:
      type: string
      format: date-time
      nullable: true
    ownerUserId:
      type: string
    planId:
      type: string
      nullable: true
    activeModuleKeys:
      type: array
      items:
        type: string
    memberLimit:
      type: integer
      nullable: true
Membership:
  type: object
  required:
    - id
    - tenantId
    - userId
    - roleKey
    - status
  properties:
    id:
      type: string
    tenantId:
      type: string
    userId:
      type: string
    roleKey:
      type: string
      enum: [tenant:owner, tenant:admin, tenant:member]
    status:
      type: string
      enum: [active, suspended]
Invitation:
  type: object
  required:
    - id
    - tenantId
    - email
    - roleKey
    - status
    - expiresAt
  properties:
    id:
      type: string
    tenantId:
      type: string
    email:
      type: string
      format: email
    roleKey:
      type: string
      enum: [tenant:owner, tenant:admin, tenant:member]
    status:
      type: string
      enum: [pending, accepted, revoked, expired]
    expiresAt:
      type: string
      format: date-time
TenantMembershipSummary:
  type: object
  required:
    - tenant
    - membership
    - isActive
  properties:
    tenant:
      $ref: '#/Tenant'
    membership:
      $ref: '#/Membership'
    isActive:
      type: boolean
CreateTenantSuccessResponse:
  type: object
  required:
    - success
    - data
    - traceId
  properties:
    success:
      type: boolean
      enum: [true]
    data:
      type: object
      required:
        - tenant
        - membership
      properties:
        tenant:
          $ref: '#/Tenant'
        membership:
          $ref: '#/Membership'
    traceId:
      type: string
ListMyTenantsSuccessResponse:
  type: object
  required:
    - success
    - data
    - traceId
  properties:
    success:
      type: boolean
      enum: [true]
    data:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/TenantMembershipSummary'
    traceId:
      type: string
SwitchTenantSuccessResponse:
  type: object
  required:
    - success
    - data
    - traceId
  properties:
    success:
      type: boolean
      enum: [true]
    data:
      type: object
      required:
        - tenant
        - membership
      properties:
        tenant:
          $ref: '#/Tenant'
        membership:
          $ref: '#/Membership'
        accessToken:
          type: string
          description: Returned for headless requests. Browser flows receive the updated access token as cookie.
    traceId:
      type: string
InvitationSuccessResponse:
  type: object
  required:
    - success
    - data
    - traceId
  properties:
    success:
      type: boolean
      enum: [true]
    data:
      type: object
      required:
        - invitation
      properties:
        invitation:
          $ref: '#/Invitation'
    traceId:
      type: string
AcceptInvitationSuccessResponse:
  type: object
  required:
    - success
    - data
    - traceId
  properties:
    success:
      type: boolean
      enum: [true]
    data:
      type: object
      required:
        - tenant
        - membership
      properties:
        tenant:
          $ref: '#/Tenant'
        membership:
          $ref: '#/Membership'
    traceId:
      type: string
TransferOwnershipSuccessResponse:
  type: object
  required:
    - success
    - data
    - traceId
  properties:
    success:
      type: boolean
      enum: [true]
    data:
      type: object
      required:
        - tenant
        - membership
      properties:
        tenant:
          $ref: '#/Tenant'
        membership:
          $ref: '#/Membership'
    traceId:
      type: string





TenantMembershipListItem:
  type: object
  required:
    - membershipId
    - userId
    - fullName
    - email
    - roleKey
    - status
    - joinedAt
    - createdAt
    - isEffectiveOwner
  properties:
    membershipId:
      type: string
    userId:
      type: string
    fullName:
      type: string
    email:
      type: string
      format: email
    roleKey:
      type: string
    status:
      type: string
      enum: [active, suspended]
    joinedAt:
      type: string
      format: date-time
      nullable: true
    createdAt:
      type: string
      format: date-time
      nullable: true
    isEffectiveOwner:
      type: boolean
TenantMembershipListSuccessResponse:
  type: object
  required:
    - success
    - data
    - traceId
  properties:
    success:
      type: boolean
      enum: [true]
    data:
      type: object
      required:
        - items
        - page
        - limit
        - total
        - totalPages
      properties:
        items:
          type: array
          items:
            $ref: '#/TenantMembershipListItem'
        page:
          type: integer
        limit:
          type: integer
        total:
          type: integer
        totalPages:
          type: integer
    traceId:
      type: string
UpdateTenantMembershipRequest:
  type: object
  minProperties: 1
  properties:
    roleKey:
      type: string
    status:
      type: string
      enum: [active, suspended]
TenantMembershipMutationSuccessResponse:
  type: object
  required:
    - success
    - data
    - traceId
  properties:
    success:
      type: boolean
      enum: [true]
    data:
      type: object
      required:
        - membership
      properties:
        membership:
          $ref: '#/TenantMembershipListItem'
    traceId:
      type: string
